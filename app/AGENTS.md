# Aseada app

Este proyecto usa **Expo SDK 51** (React 18.2, React Native 0.74.5).

Lee la documentación de esa versión exacta antes de escribir código:
https://docs.expo.dev/versions/v51.0.0/

No uses APIs de SDK 52+ ni de React 19. Si una dependencia solo existe
para una versión posterior del SDK, no la agregues.
