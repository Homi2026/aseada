node_modules/

# Variables de entorno: cubre .env, .env.local, .env.production, etc.
# `vercel env pull` escribe .env.local con secretos de produccion.
.env*
!.env.example

.vercel

# Skills que instala una herramienta local de agentes (unos 6 MB de
# documentacion de Neon). No son parte del backend ni las necesita el deploy.
.agents/
.claude/
skills-lock.json
